[project]
name = "a2z-agent-hire"
version = "0.3.0"
description = "Human-controlled job board and evaluated agent-work exchange"
requires-python = ">=3.11"

[project.scripts]
a2z-agent-hire = "apps.api.server:main"

[tool.setuptools.packages.find]
where = ["packages/oss", "packages/commercial"]
